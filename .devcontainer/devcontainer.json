{
  "name": "Go CLI Dev",
  "image": "golang:1.22",
  "features": {
    "ghcr.io/devcontainers/features/git:1": {}
  },
  "containerEnv": {
    "LANG": "en_US.UTF-8",
    "LC_ALL": "en_US.UTF-8"
  },
  "postCreateCommand": "apt-get -y update && apt-get install -y curl && curl -LsSf https://astral.sh/uv/install.sh | sh && ~/.local/bin/uv tool install pre-commit && go install github.com/spf13/cobra-cli@latest && go install github.com/go-delve/delve/cmd/dlv@latest"
}
